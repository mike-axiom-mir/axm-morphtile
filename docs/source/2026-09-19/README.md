# Direction sources — 2026-09-19

These are the four distinct source notes supplied after the verified v0.4 checkpoint. They are kept verbatim so later implementation can be checked against the original wording.

| Source | Current disposition |
| --- | --- |
| `AXM_MorphTile_Interface_Is_Matter_Codex_Build_Note.txt` | The minimum portable placement contract is implemented as `tile.presentation`; session overrides remain non-canonical, spatial anchors resolve through actual frames, and unsupported host modes HOLD. Interactive website authority remains open. |
| `AXM_MorphTile_Universal_Spatial_Anchor_Codex_Build_Note.txt` | The identity world root, recursive local position/rotation, destination-placed kit import, instance-local frames and transform-preserving collapse/expand are implemented and adversarially tested. |
| `AXM_MorphTile_World1_Shop_Market_Commerce_Log.txt` | Implemented as a separate World #1 policy module, not a MorphTile default: auction cuts, co-present direct trade, persistent popup stores, deterministic settlement and bounded AI proposals. |
| `AXM_MorphTile_Cold_Matter_Memory_Arbitrage_Log.txt` | Implemented as an experimental local-file adapter and bounded measurement. Selective residency and exact reconstruction are proven for the fixture; the current run lowers JS heap but raises retained RSS, so no universal RAM claim is made. |

The status above is backed by `test/spatial.js`, `test/presentation.js`, `test/world1-commerce.js`,
`test/cold-matter.js`, and `evidence/cold-matter-measurement.json`. The source notes themselves remain verbatim.
